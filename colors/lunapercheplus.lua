-- lunapercheplus: the built-in "lunaperche" colorscheme with the modern
-- highlight groups it lacks added using catppuccin's color-assignment logic,
-- backed by lunaperche's palette. See lua/retro/init.lua. Pure Lua.
require("retro").load({ name = "lunapercheplus", base = "lunaperche", flavor = "plus" })
